<h1>Badges</h1>

<%= render 'shared/errors', resource: @bage %>

<% if current_user.user_bages.present? %>
<table class="table-sm table-striped table-bordered">
  <thead class="thead-inverse">
    <tr>
    <th>Name</th>
    <th>Icon</th>
    </tr>
  </thead>

  <%= render @bage %>

</table>
<% else %>
  <p>You have no badges</p>
<% end %>

<%= link_to "Back", admin_bages_path %>
