<h1><%= t('.header') %></h1>

<% if @tests.present? %>
<table class="table table-reflow">
  <thead class="thead-inverse">
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.level') %></th>
    <th><%= t('.action') %></th>
  </tr>
</thead>

  <%= render @tests%>

</table>
<% else %>
  <p>t('.zero')</p>
<% end %>
