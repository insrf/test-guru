<h1><%= t('.header') %></h1>

<% if @tests.present? %>
<table>
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.level') %></th>
    <th><%= t('.action') %></th>
  </tr>

  <%= render @tests%>

</table>
<% else %>
  <p>t('.zero')</p>
<% end %>
